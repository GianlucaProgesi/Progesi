name: CI

on:
  push:
    branches: [ main, "feature/**", "chore/**", "tests/**" ]
    paths-ignore:
      - "**/*.md"
      - "**/*.png"
      - "**/*.jpg"
      - "**/*.gif"
  pull_request:
    branches: [ main ]

jobs:
  build-test:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET SDKs
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            6.0.x

      - name: Cache NuGet
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore
        run: dotnet restore

      - name: Build (Release)
        run: dotnet build --configuration Release --no-restore

      - name: Test (x64 + coverage)
        run: dotnet test --configuration Release --no-build --collect:"XPlat Code Coverage" -- RunConfiguration.TargetPlatform=x64 -l "trx;LogFileName=test.trx" --verbosity normal

      - name: Generate coverage report
        uses: danielpalme/ReportGenerator-GitHub-Action@v5
        with:
          reports: "**/coverage.cobertura.xml"
          targetdir: "coveragereport"
          reporttypes: "HtmlInline;TextSummary"
          historydir: "coveragereport-history"

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coveragereport

      - name: Publish test results (trx)
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: "VSTest Results"
          path: "**/test.trx"
          reporter: dotnet-trx
